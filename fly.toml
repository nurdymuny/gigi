app = "gigi-stream"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[deploy]
  strategy = "bluegreen"

[env]
  PORT = "3142"
  GIGI_DATA_DIR = "/data"

[http_service]
  internal_port = 3142
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  auto_stop_threshold = "off"

[checks]
  [checks.readiness]
    type = "http"
    port = 3142
    path = "/v1/health"
    interval = "5s"
    timeout = "3s"
    grace_period = "600s"

[[vm]]
  memory = "32gb"
  cpu_kind = "performance"
  cpus = 4

[mounts]
  source = "gigi_data"
  destination = "/data"
