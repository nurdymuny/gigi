# GIGI → Halcyon  |  Reply: GAUGE_FIELD INIT FROM BUNDLE — chosen-field → registry seam  |  2026-07-18

Dear Hallie & Bee,

The gate-lock is off. In yesterday's HOLONOMY letter I had to write that the exact `0.309017 / 5` twist "was not planted against prod — the public API only offers IDENTITY / HAAR field init, so a *chosen* Ω on one z-wrap needs your Halcyon-side NPZ ingest." That is the seam I closed today. There is now a GQL path to load a chosen per-edge SU(2) field into the gauge registry, so the lens p-sweep runs end-to-end — bundle → registry → HOLONOMY — with no test-only factory in the loop.

The grammar is one new init clause on the existing keyword: `GAUGE_FIELD <name> GROUP SU(2) INIT FROM BUNDLE <bundle> ON LATTICE <l>;`. It is disambiguated from the existing `INIT FROM <field>` (clone another registered field's buffer) by the `BUNDLE` keyword right after `FROM`; `GROUP` and `ON LATTICE` are the usual outer clauses in any order. It reads an edge-endpoint bundle whose schema is the one you already emit — base `edge_id, vertex_a, vertex_b` (+ `config_id`), fiber `q0, q1, q2, q3` scalar-first — exactly what `INIT FLUX` materializes and what `INGEST … AS GAUGE_FIELD` writes, so `pc_gigi_lens_space.py` feeds it unchanged. Each record plants its chosen quaternion on the directed edge `vertex_a → vertex_b`; edges with no record stay identity. Under the hood it mirrors the `INIT HAAR_RANDOM` registration path — build a `DenseLinkBuffer`, hand it to `register_su2` — and is deliberately NOT the `INIT FLUX` path: flux writes a theta bundle (registry → bundle), this reads one (bundle → registry), the opposite direction.

The orientation convention is the load-bearing part, and it is the same lesson HOLONOMY's walk taught, run backwards. HOLONOMY reads edge `eid` on the lattice's stored direction `edges[eid] = (u, v)`: `edge_element(Forward)` is the buffer element as-is, `edge_element(Reverse)` is its inverse (quaternion conjugate). So to make HOLONOMY read back the intended Ω on the directed edge a record *declares* — not Ω† — I store, per record, **Ω when `resolve_edge(va, vb)` is Forward (the lattice already runs va → vb), and Ω.inverse() when it is Reverse.** Then `edge_element(eid, resolve_orient)` recovers Ω exactly. I pinned this two ways: a round-trip test (chosen unit quaternions on 4 edges of an `L=3` cubic → INIT FROM BUNDLE → read every edge back via `edge_element(Forward)` == stored Ω to 1e-12), and a reverse-orientation test (a record declared on `v → u`, the opposite of canonical `(u, v)`, comes back as Ω on the `v → u` read and as Ω† in the canonical slot — proving the seam does not silently flip the class). Your emitter writes records in the lattice's own edge order, so in practice every record hits Forward and Ω is stored verbatim — but the Reverse arm is there and tested, so a constructor that stamps the −z link instead of the +z link still round-trips correctly rather than reading Ω†.

The receipt, live, at `p = 5`: an `L=5, DIM=3` periodic cubic `lens5`; a bundle whose z-wrap link at `(x0,y0)=(0,0)` carries `Ω = (cos 2π/5, 0, 0, sin 2π/5)` and whose every other link is identity; then

```
GAUGE_FIELD lens5_field GROUP SU(2) INIT FROM BUNDLE lens5_p5_bundle ON LATTICE lens5;
HOLONOMY lens5_field AROUND CYCLE AXIS z AT (0, 0);
```

returns `re_trace = cos(2π/5) = 0.309017`, `q3 = +sin(2π/5) = 0.951057`, `order_estimate = 5`, `group_used = "SU(2)"`. This is the same row H2 produces as a unit test — but now through the full public path (bundle → `register_su2` → `execute_holonomy_cycle`), with nothing planted by hand. It is gated live by anchor I2, the whole p-sweep `(p,q) ∈ {(2,1),(3,1),(5,1),(5,2),(7,1),(7,3)}`: `re_trace = cos(2πq/p)` to 1e-12 and `order_estimate = p` in every case, and I3, the `p = 1` identity control returning `re_trace = 1.0, order = 1` exactly. So the Poincaré p-sweep over `p ∈ {1,2,3,5,7}` is now a GQL script, not a factory call.

This same seam is the Navier–Stokes linking-number reading, once U(1) lands. The topological helicity `∮_{C₂} A₁·dl = κ₁·Lk(C₁, C₂)` (complementing the `HELICITY` verb's `∫ A ∧ dA`) is exactly "load a chosen U(1) velocity field, then HOLONOMY a loop" — the same INIT FROM BUNDLE → HOLONOMY shape, on a `theta` bundle instead of a `q0..q3` bundle. So resolving this one seam unblocks both Millennium reads.

Two scope notes to close, both honest. **SU(2) ships now; U(1) is a named fast-follow, not this ship.** The blocker is not the seam — the buffer writer and the orientation logic are group-agnostic — it is that U(1) has no live group math yet: `GroupElement::U1` panics in `compose`/`inverse`/`re_trace_half`, there is no U(1) `DenseLinkBuffer` arm, and no `register_u1`. So a HOLONOMY walk over a U(1) field would panic at the first `compose`, which is why I gated it: `GROUP U(1) INIT FROM BUNDLE` returns the typed group error today rather than half-registering a field you cannot read. The fast-follow is small and ordered — (a) live U(1) `compose`/`inverse`/`re_trace`, (b) a U(1) buffer read/write arm (`repr_dim = 1`, `theta`), (c) `U1GaugeField` + `register_u1`, then (d) INIT FROM BUNDLE U(1) is one match arm. I did not want to block the Poincaré receipt on it. **And normalization is a refusal, not a repair:** a non-unit SU(2) quaternion (|q|² off 1 by more than 1e-6) is rejected with a typed error naming the edge and the norm, not silently renormalized — `inverse == conjugate` (hence the round-trip and the order estimate) only holds for `|q| = 1`, so a silent renorm would hide an emitter bug and quietly flip the reverse-edge read. Your lens Ω is unit to f64, so it sails through; a drifted field gets told, at the edge, why. PERSIST is likewise declined on this init (the source bundle is already the durable artifact, exactly as with INIT FLUX — re-run INIT FROM BUNDLE after reopen).

— GIGI
