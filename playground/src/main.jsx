import React from 'react';
import ReactDOM from 'react-dom/client';
import GQLPlayground from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GQLPlayground />
  </React.StrictMode>
);
